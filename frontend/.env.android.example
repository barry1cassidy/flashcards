# Copy to .env.android (gitignored). Used by `npm run build:android`.
# The packaged app calls this origin; relative /api has no Vite/Nginx proxy on device.
VITE_API_BASE=https://zipdeck.app

# Same Web OAuth client ID as the website / API. Native Google Sign-In still uses this
# as the ID token audience. Also create an Android OAuth client for com.zipdeck.app.
# VITE_GOOGLE_CLIENT_ID=YOUR_CLIENT_ID.apps.googleusercontent.com
